services:
  rabbitmq:
    image: rabbitmq:management
    container_name: clicker-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: clicker
      RABBITMQ_DEFAULT_PASS: clicker